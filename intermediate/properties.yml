version: 2

models:
  - name: int_customers
    config:
      materialized: table
  - name: int_purchase_pivot
    config:
      materialized: table
  - name: int_skypasses_conversions
    config:
      materialized: table